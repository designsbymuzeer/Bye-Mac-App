name: "CodeQL Security Scan"

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  analyze:
    name: Analyze
    # We must use macOS because we are building an Xcode project
    runs-on: macos-latest
    permissions:
      actions: read
      contents: read
      security-events: write

    strategy:
      fail-fast: false
      matrix:
        language: [ 'swift' ]

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    # Initialize CodeQL with "manual" build mode
    - name: Initialize CodeQL
      uses: github/codeql-action/init@v3
      with:
        languages: ${{ matrix.language }}
        build-mode: manual

    # THIS IS THE PART THAT FIXES YOUR ERROR
    # We tell GitHub exactly how to build the app without signing it
    - name: Build macOS App
      env:
        scheme: "Bye" # <--- REPLACE THIS WITH YOUR SCHEME NAME
      run: |
        xcodebuild -scheme "$scheme" clean build \
        CODE_SIGN_IDENTITY="" \
        CODE_SIGNING_REQUIRED=NO \
        CODE_SIGNING_ALLOWED=NO

    - name: Perform CodeQL Analysis
      uses: github/codeql-action/analyze@v3
      with:
        category: "/language:${{matrix.language}}"
